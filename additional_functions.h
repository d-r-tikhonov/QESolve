#ifndef __ADDITIONAL_FUNCTIONS_H__
#define __ADDITIONAL_FUNCTIONS_H__


/// 					Checks whether the number is in the neighborhood of zero
/// \param n 			The number to be checked
/// \param equivalent 	The number with which n is compared
/// \return 			True if the number lies in the neighborhood of the number, false otherwise
bool isEqFloat(float n, float equivalent);


void clearBuffer(void);

#endif